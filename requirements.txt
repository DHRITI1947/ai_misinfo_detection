# Pinned dependencies known to work with this project
transformers==5.3.0
# Pin torch to a version compatible with torchvision==0.22.1 (resolved conflict)
torch==2.7.1
torchvision==0.22.1
accelerate>=1.1.0
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.3.2
flask==2.3.3
nltk==3.8.1

# Optional / dev
# pytest
# black
